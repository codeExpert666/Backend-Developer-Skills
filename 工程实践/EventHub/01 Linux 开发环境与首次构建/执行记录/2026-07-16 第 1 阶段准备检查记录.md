---
title: 2026-07-16 第 1 阶段准备检查记录
aliases:
  - EventHub 第 1 阶段初始环境审计
tags:
  - 工程实践
  - EventHub
  - 执行记录
  - Linux
  - 准备检查
created: 2026-07-17T00:48:00
updated: 2026-07-17T01:12:07
---

本文保存 2026-07-16 编写第 1 阶段笔记前的只读事实快照。它只用于说明为什么选择当前资源、版本和迁移路线，不代表虚拟机已经创建或项目已经在 Linux 中验证通过。

> [!warning] 记录边界
> 本记录不包含真实用户名、IP、密钥、令牌、密码或具体未提交业务文件名。仓库和机器状态会变化；重新实施时必须获取新的事实快照，不应继续把本记录当作当前状态。

## 宿主机检查

| 项目 | 2026-07-16 观察结果 | 对第 1 阶段的影响 |
| --- | --- | --- |
| 芯片与架构 | Apple M4，ARM64 | 选择 Ubuntu Server ARM64 和 UTM Virtualize |
| 内存 | 24GB | 候选为 VM 分配 12GB，同时为 macOS 留出余量 |
| 根卷可用空间 | 约 347GiB | 允许以 180GB 稀疏磁盘作为候选上限，创建前仍需重查 |
| UTM | 未安装 | 笔记阶段不能声称 VM 已建立 |
| OrbStack | 已安装，版本 2.2.1 | 不作为完整 Ubuntu Server VM 主线 |

没有记录设备序列号、硬件 UUID 或其他唯一标识。

## EventHub Go 仓库检查

| 项目 | 观察结果 |
| --- | --- |
| 当前分支 | `main` |
| 本地 HEAD | `45e80f9b814ce9937be9b3120ed48efdac8e1fda` |
| 当时远端 `main` | `40213fc51e454e3662935948138dc555a618125c` |
| 提交关系 | 本地领先远端 1 个提交 |
| 工作区 | 存在 1 个已跟踪未暂存修改；具体文件名不进入公开记录 |
| Go 版本 | `go.mod` 声明 `1.24.0` |
| 主要质量入口 | Makefile、CI、OpenAPI、Dockerfile 和 Compose 均被核对 |

迁移结论：fresh clone 只能获得远端提交，不能获得本地领先提交和工作区修改。真正迁移前必须重新检查状态，并选择先提交/push、Git bundle + patch 或受控工作区复制。

## EventHub Java 仓库检查

| 项目 | 观察结果 |
| --- | --- |
| 当前分支 | `main` |
| 本地 HEAD | `b11a06bca0e74d33266fd2b2c83c685779df9ec0` |
| 当时远端 `main` | 与本地 HEAD 相同 |
| 工作区 | 存在已跟踪未提交修改和未跟踪工程文件；具体业务文件名不进入公开记录 |
| JDK 版本 | POM 与 README 要求 JDK 21 |
| Maven 基线 | 远端 revision 要求 Maven 3.9+ |
| 本地附加入口 | 当时本地工作区存在未跟踪的工程入口与质量配置；具体文件名不在本记录展开 |

迁移结论：fresh clone 不会得到这些未跟踪工程入口，不能在 Linux 中直接假设 `./mvnw` 或 `make ci` 存在。需要先检查当前 revision 的跟踪状态。

## 只读验证边界

准备检查期间完成了：

- 读取两个仓库的 Git 状态、分支、远程和提交。
- 读取 Go `go.mod`、README、Makefile、CI、Dockerfile 和 Compose。
- 读取 Java `pom.xml`、README、Wrapper、Makefile、CI 和静态分析配置状态。
- 对两个项目现有 Compose 文件做静态解析检查。
- 核对 Go 1.24.0 Linux ARM64 官方归档 SHA-256。

准备检查期间没有：

- 安装 UTM。
- 创建或修改虚拟机。
- 修改 macOS、Linux、SSH、Tailscale 或 Docker 配置。
- 修改两个项目。
- 在 Linux 虚拟机中运行构建。
- 声称质量门禁已经通过。

## 下一次实施前重新检查

**执行位置：macOS 宿主机（各项目根目录）**

```bash
git status --short --branch
git remote -v
git branch --show-current
git rev-parse HEAD
git fetch --prune origin
git rev-list --left-right --count '@{upstream}...HEAD'
```

**执行位置：macOS 宿主机（任意目录）**

```bash
uname -m
system_profiler SPHardwareDataType | sed -n '/Chip:/p;/Memory:/p'
df -h /
test -d /Applications/UTM.app && printf 'UTM installed\n' || printf 'UTM not installed\n'
```

新的检查结果应写入新的执行记录，不覆盖本记录。

## 关联笔记

- [[EventHub 六阶段工程化实践路线图]]
- [[EventHub 第 1 阶段实施概览]]
- [[EventHub 第 1 阶段环境与版本基线]]
- [[EventHub 仓库迁移与首次质量门禁]]
- [[EventHub 第 1 阶段验收清单]]
