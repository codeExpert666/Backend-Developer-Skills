# Backend-Developer-Skills 仓库协作规则

## 1. 适用范围、约束词与优先级

本文件适用于仓库根目录及全部子目录，用于约束对 Obsidian 笔记、学习记录、源码研读记录和项目实践文档的调查、撰写、修改与验证。

- **必须（MUST）**：不可省略；若无法满足，停止交付并说明原因。
- **应该（SHOULD）**：默认执行；只有存在明确、可说明的上下文理由时才可偏离。
- **可以（MAY）**：按任务价值和风险选择。

在遵守系统、平台、工具权限和安全约束的前提下，仓库内规则发生冲突时按以下顺序处理：

1. 当前用户的明确要求和范围边界。
2. 目标目录内更深层、适用范围更窄的 `AGENTS.md`。
3. 本根级 `AGENTS.md`。
4. 目标笔记已经声明的职责、相邻笔记的稳定风格和通用默认习惯。

更具体的规则不得扩大用户请求，也不得绕过工作区安全、事实证据或秘密保护要求。若笔记与项目当前代码事实冲突，项目当前代码、配置、迁移、契约和测试优先；旧笔记只能作为历史材料。无法可靠裁定时，必须明确标出冲突、未核实项和需要的验证，不得静默拼接为确定结论。

`AGENTS.md`、README 和配置文件属于仓库协作或说明资产，不是普通 Obsidian 知识笔记；除非它们明确作为笔记使用，否则不强制添加笔记 Frontmatter。

## 2. 仓库定位与内容目标

这是一个面向后端开发学习、通用技术知识、GitHub 源码研读和真实项目实践的 Obsidian 笔记仓库。

- 通用知识必须追求跨项目、跨机器复用价值，帮助读者理解原理、完成实操、验证结果、定位失败并迁移到新环境。
- `项目实践/` 只保存特定项目的能力组合、决策、边界、实施顺序和验收，不重复维护通用教程。
- 笔记不仅要“保存资料”，还应支持学习、复现、验证、排障、恢复和迁移。
- 不得把仓库写成命令摘抄、链接收藏、未经核对的 AI 文本堆积，或项目代码仓库的低质量镜像。
- 概览负责建立地图和阅读入口，专题笔记负责形成可解释、可操作的知识闭环。

## 3. 用户请求模式与范围控制

### 3.1 分析模式

当用户要求“审视”“判断是否合理”“给出方案”“是否有必要调整”或表达同等含义时：

- 必须先调查并给出判断、依据、范围、风险和建议顺序。
- 未经用户批准，不得修改、重命名、移动或删除文件。
- 可以执行安全的只读检查，但不得把分析请求自动升级为实施请求。

### 3.2 执行模式

当用户说“同意”“请执行”“请实现”“请修复”“请完成”或表达同等含义时：

- 在范围已经清晰后必须直接实施并完成与风险相匹配的验证。
- 不得重复讨论已获批准的方案，也不得要求用户再次确认非关键目录名、文件名或可从仓库查明的事实。
- 只有缺失信息会实质改变范围、结果、外部影响或所需权限时，才暂停并请求决定。

任何模式下都必须保持请求范围：注释、措辞、Frontmatter、单篇笔记、目录评估或文档任务，不得扩张为无关的目录重构、全库重写、源码修改或发布操作。发现范围外问题时只记录并报告，除非用户明确纳入范围。

## 4. 工作区与 Git 安全

开始任何编辑前必须：

1. 确认当前工作目录和仓库根目录。
2. 运行 `git status --short --branch`，识别已暂存、未暂存、未跟踪和删除项。
3. 查找从根目录到目标目录适用的全部 `AGENTS.md`。
4. 读取目标文件、直接关联文件和相邻代表性笔记，确认职责与当前真实目录名。

工作区规则：

- 现有修改默认全部属于用户，必须保留；不得覆盖、混入、清理、还原或据为本次成果。
- 禁止使用 `git reset --hard`、`git clean -fd`、无明确目标的 `git restore`、`git checkout --` 或等价破坏性命令清理现场。
- 不得为让检查通过而删除失败证据、生成物或用户文件。
- 不得顺手修复范围外内容；批量替换、重命名和格式化前必须证明目标集合完全属于请求范围。
- 未经用户明确要求，不得暂存、提交、推送、创建分支或发布。
- 修改后必须再次检查 Git 状态，并把编辑前后状态对照；最终报告本次修改和已存在但未处理的范围外修改。
- 笔记任务通常只授权读取关联代码仓库；除非用户明确要求修改源码，不得对代码仓库执行写操作。

公开笔记和协作规则中不得写入当前未提交内容、无必要的个人绝对路径、动态 IP、设备唯一标识、密钥、Token、密码、私钥、Cookie、真实凭据、命令历史或私人数据。需要说明路径时优先使用仓库相对路径、`$HOME`、`$USER`、主机别名或含义明确的变量。

`.obsidian/`、`.claudian/` 等被忽略的本地工作区、插件和会话数据不是知识事实来源；除非任务明确要求维护本地配置，否则不得修改或纳入提交范围。

## 5. 信息架构与事实来源

写入前必须先用当前文件系统确认真实目录和文件名。当前项目实践根目录是 `项目实践/`；不得凭旧记录沿用已经不存在的目录名称。

仓库内容按以下职责分层：

| 类型 | 当前归档方向 | 负责内容 | 不负责内容 |
| --- | --- | --- | --- |
| 路线图或总览 | 技术主题入口、`AI/` 路线、`项目实践/` 内总览与路线图 | 目标、依赖、顺序、进入条件、退出标准、状态和导航 | 完整教程、单次执行结果 |
| 可复用通用知识 | `Linux/`、`Git/`、`Docker/`、`Java/`、`Go/`、`MySQL/`、`Terminal/`、`虚拟化/`、`AI/` 等主题目录 | 原理、选择依据、操作、验证、排障和迁移 | 特定项目的 SHA、临时环境和业务决策 |
| GitHub 通用源码导读 | `AI/GitHub 项目研读/` 等已有主题位置 | 仓库地图、公开入口、调用链、测试入口和通用阅读方法 | 虚构的个人研读进度或未发生的运行结果 |
| 特定版本与问题的源码研读记录 | 已实际开始的相应学习记录位置 | 日期、问题、release/tag/commit、调用链、运行或测试证据、结论 | 无证据的“已研读”声明 |
| 阶段学习与实验记录 | `AI/学习记录/` 及真正开始后创建的阶段结构 | 个人学习进度、固定输入、成功与失败、评测、恢复、迁移结论 | 通用原理的重复副本 |
| 项目实践笔记 | `项目实践/<项目>/` | 项目特有的产品能力、工程组合、约束、取舍、实施顺序和验收 | 跨项目技术教程 |
| 项目执行记录 | 所属项目阶段或能力下的 `执行记录/` | 某次版本、提交、环境、命令、指标、结果、失败和恢复点 | 稳定教程或未经执行的预期 |
| 代码与实验产物 | 对应代码或实验仓库 | 可运行代码、数据集、JSONL、完整日志、Trace 和大规模评测输出 | 在笔记仓库复制整套原始产物 |

归档与单一事实来源规则：

- 路线图必须说明“为什么、依赖什么、按什么顺序、何时进入、何时退出”，但不承担完整教程职责。
- 通用知识笔记按技术问题组织，不得机械复制某条学习路线的阶段目录。
- 学习记录可以按阶段组织，因为它表达真实个人进度；只有实际开始阶段时才创建对应内容。
- 项目实践按产品能力或工程维度组织，不得机械复制通用学习阶段。
- 通用原理必须只在通用笔记维护一份事实来源；项目笔记通过 `[[Note Name]]` 引用，只补项目差异、决策和验收。
- 项目总览维护跨路线关系、术语和导航；子路线不得各自复制一份容易漂移的总表。
- 多条路线都使用阶段编号时，必须写出限定词，例如“业务阶段”“工程化第 N 阶段”“AI 阶段”，不得只写含糊的“第 N 阶段”。
- 执行记录必须日期化并保留历史；新的实施生成新记录，不得覆盖旧记录来伪装当前状态。
- 原始代码、JSONL、完整日志和大型评测结果应留在代码或实验仓库；笔记只保存版本、相对路径或可定位标识、摘要、指标和结论。
- 不得预建未来阶段的空目录、空笔记、空接口或占位 Wikilink。新目录应与第一篇有实质内容的笔记同时出现。

## 6. Codex 与用户的协作边界

总体原则：**Codex 负责调查、讲解、初稿、结构化、校对和证据整理；用户负责提问、复现、判断、决策和最终验收。**

- 通用知识笔记可以由 Codex 主写，但“笔记已完成”不得被表述为“用户已掌握”。
- GitHub 通用源码导读可以由 Codex 主写，但必须以真实仓库结构和可核对来源为依据。
- 特定日期、版本和问题的源码研读记录必须存在真实阅读、运行、测试或其他可重放证据；Codex 不得替用户虚构“已研读”。
- 学习记录必须来自真实实验。Codex 可以读取代码、终端输出、日志和评测结果后整理成文，但不得编造运行结果、失败经历、个人感受、理解程度或掌握状态。
- 项目实践适合人机协作：Codex 可以调查仓库、提取当前事实、解释边界并起草方案；用户负责产品边界、关键技术取舍和最终验收。
- ADR 可以由 Codex 起草候选方案、收益、代价、证据和重评条件；“采用什么以及为什么”的最终决定必须得到用户确认。确认前必须标为候选或待确认，不得写成已采用。
- “资料已经整理”“官方来源已核对”“本地实验已验证”和“用户已经掌握”是不同状态，必须分别表达。

需要跟踪状态时，可以使用但不强制所有笔记使用以下两组状态：

- 资料状态：`Codex 初稿` / `官方来源已核对` / `本地实验已验证`。
- 掌握状态：`未学习` / `能解释` / `能实现` / `能排障` / `能迁移`。

未经用户真实复现和确认，不得自动提高掌握状态。任何“已完成”“已通过”“已实现”“可发布”声明都必须由文件、代码、测试、命令输出、截图、Trace、评测结果或恢复记录支撑；证据不足时必须写“未验证”“部分验证”或“证据不足”。

## 7. 笔记写作原则

- 默认使用中文。首次出现的英文术语、缩写、工具或配置名必须先解释其含义、作用和边界。
- 面向学习者时，必须先说明概念、角色、边界、适用条件和触发条件，再给命令、配置或实现。
- 优先采用“概念模型 → 操作或实现 → 验证 → 失败与恢复”的学习闭环；可按主题裁剪，不得为了统一而强迫所有笔记拥有完全相同的章节。
- 一篇笔记只回答一个具有内聚性的主要问题。避免“一词一篇”的过度拆分，也避免把多个独立主题塞入难以维护的巨型笔记。
- 概览负责选择路径和导航；专题笔记负责深入解释。概览不得吞并全部细节，专题也不得复制整份路线图。
- 文件名和标题必须表达具体问题或能力，不使用“笔记 1”“学习总结”“其他知识”等模糊名称。
- 通用笔记的标题、aliases、tags、正文和示例必须保持项目中立。项目实体、项目包名、项目版本和业务红线只进入项目实践或明确标注的案例。
- 示例业务对象应保持中性；不得把 EventHub 或其他项目实体伪装为所有项目的通用答案。
- Java 与 Go 示例应共享产品语义而不是代码形状：Java 遵循 Java 生态与语言习惯，Go 遵循 Go idiom，不逐行互译异常、并发、依赖注入或包结构。
- 写作必须帮助读者举一反三，不绑定单台机器、单次终端会话或一次性目录。
- 正常主流程与异常、FAQ、排障和恢复应分开组织；危险操作必须说明前置检查、影响范围、验证和恢复方法。
- 掌握分级、完成标准、相关笔记和官方资料是按需使用的结构，不是每篇笔记都必须填满的僵化模板。

## 8. Obsidian 与 Markdown 规范

### 8.1 Frontmatter 与命名

- 所有作为 Obsidian 笔记维护的 Markdown，包括通用知识、学习记录、源码研读、项目实践和执行记录，都必须保留合法 YAML Frontmatter；不得破坏开头和结尾的 `---` 围栏、缩进、列表或字符串语义。
- 新笔记应该沿用仓库现有的 `title`、`aliases`、`tags`、`created`、`updated` 字段结构，但不得为了填字段编造 alias 或无意义标签。
- `created` 和 `updated` 使用秒级格式 `YYYY-MM-DDTHH:mm:ss`。
- 修改旧笔记时必须保留原 `created`，并把 `updated` 更新为本次实际修改时间；不得只为刷新时间而改文件。
- 笔记文件名去掉 `.md` 后必须与 Frontmatter `title` 一致，且二者都要符合正文职责。
- 重命名时应检查入链；旧名称仍有发现价值时，通过 `aliases` 保留，而不是制造同内容副本。
- 既有 Frontmatter 字段、标签粒度和时间精度应与相邻笔记保持一致；不得无任务依据发明全库新字段。

### 8.2 链接、表格与公式

- 内部链接必须使用 `[[Note Name]]`；带标题、锚点或显示文本时也必须保证目标真实存在且可解析。
- 不得用反引号包裹需要点击的 Wikilink；表格中的带显示文本链接使用 `[[Target\|Label]]`，保证 Obsidian 与 Markdown 表格都能解析。
- 新增链接前必须按文件名和 `aliases` 核对目标；不得创建指向未来文件或错误标题的 Wikilink。
- Markdown 表格前必须保留一个空行。
- 行内数学公式使用 `$...$`，块级公式使用 `$$...$$`，不得改用 Obsidian 不兼容的替代定界形式。
- 外部来源使用普通 Markdown 链接，并尽量直达支撑当前事实的一手页面。

### 8.3 Mermaid、代码与 Shell

- 所有代码围栏必须闭合，声明语言必须与内容匹配；不可运行的调用链或语法骨架使用 `text`，不要伪装成可运行 Shell。
- Mermaid 代码块必须闭合并保持基本语法有效；节点文本含空格、括号、标点、换行标记或其他易歧义字符时必须使用引号。
- 可执行 Shell 代码应按风险和上下文说明执行环境或位置、前置条件、预期结果及必要恢复；有副作用的片段必须完整说明这些边界。
- Shell 代码块中不得使用会被 Shell 解释的裸尖括号占位符。应使用含义明确的变量、交互输入或把纯语法骨架改为 `text`。
- 命令示例不得包含真实秘密；敏感值只写环境变量名或 Secret 来源，不写示例真值。
- 有状态、覆盖、删除、迁移、权限或安全影响的命令必须先给只读检查，并把正常流程与回滚流程分开。

## 9. 来源、时效与源码研读

- 技术事实优先使用官方文档、官方规范、官方仓库、公开 API 和测试。
- 技术问题需要联网核对时，必须优先使用一手来源；第三方文章只可补充解释或发现线索。
- 易变化的版本、模型、价格、默认配置、兼容矩阵、协议成熟度和安全规范必须记录核对日期。必要时同时记录运行时、框架、规范和目标项目版本。
- GitHub 源码研读必须记录 release、tag 或 commit；默认分支只能用于观察后续设计，不得被当作稳定版本或兼容性结论。
- 源码行为证据优先级为：规范与公开 API → conformance 或自动化/一致性测试 → 官方示例 → README。
- Issue、Discussion、博客和第三方教程可以用于发现问题，但不得单独证明契约、兼容性或安全结论。
- 源码导读必须区分手写核心、生成代码、示例、第三方适配层和测试；只跟踪与当前工程问题有关的最小调用链。
- 一轮具体源码研读应该只回答一个主要工程问题，至少包含版本快照、停止条件、正常路径、失败路径、测试证据和可迁移结论。
- 不得复制大段受版权保护的源码或文档。优先写自己的解释、结构地图、调用链和必要的短片段，并标明来源。
- 当前无法可靠核实的内容必须标记为“未核实”“待验证”或“仅为推断”，同时给出重新核对方法；不得把猜测写成确定结论。

## 10. 项目实践规则

- 项目当前行为必须以目标代码仓库当前工作树及其代码、配置、数据库迁移、测试、公开契约、构建入口和 CI 为事实来源；需要形成可复现基线时，还必须落到可定位的 revision，并单独记录未提交差异。
- 编写或更新项目实践前，必须检查目标代码仓库当前 Git 状态、revision 和相关工程入口；不得只依赖旧笔记、旧执行记录、历史 HEAD 或内存。
- 必须区分“本地存在”和“已被 Git 跟踪”：fresh clone 只能获得远端可达的已提交内容，不能把未跟踪或仅本地文件当成项目基线。
- 项目笔记只记录项目选择、项目约束、业务门槛、实现顺序、差异和验收；AI、Linux、Docker、Git、Java、Go 等通用原理通过 Wikilink 引用，不在项目笔记重复展开。
- 项目特有的精确版本、资源或状态必须注明核对日期、来源、适用范围和重新验证方法；项目声明、Wrapper、构建文件和 CI 优先于“最新版”。
- 对项目写操作、身份、权限、事务、幂等、状态机和人工确认边界，AI 只能解释或提出受控意图，不得推测、绕过或宣称已执行。
- 未实现、未运行、未测试或未验收的能力不得写成已完成。未来路线必须明确是计划或候选，不得伪装成当前能力。
- 项目执行记录与通用教程必须严格分离。执行手册写预期和方法；日期化记录写实际命令、版本、完整提交标识、退出码、PASS/SKIP、失败原因、残留风险和恢复点。
- 退出码为 0、静态配置解析、编译、测试、镜像构建、部署和健康检查是不同证据层级，不得互相替代。未执行和跳过项必须如实记录。
- 会改写源码或生成物的项目门禁应在干净、可恢复的 checkout 或隔离工作树中运行，并比较执行前后 Git 状态；文档任务不得擅自创建这种源码改动。
- Java 与 Go 对照项目可以共享业务语义、输入输出、错误分类和评测数据，但各自实现必须遵循本语言和项目的现有边界。

## 11. 修改流程

每次仓库任务应按以下闭环执行：

1. **调查**：确认请求模式、适用规则、Git 现场、真实目录、目标职责和关联事实来源。
2. **定界**：列出本次允许修改的文件和明确不处理的事项；保护用户现有修改。
3. **取证**：读取代表性笔记、必要的官方资料或当前项目代码；记录易变事实的日期与版本。
4. **撰写**：先写概念和边界，再写操作、验证及失败恢复；复用既有事实来源和 Wikilink。
5. **复核**：检查事实、措辞、范围、Frontmatter、链接、代码与秘密；不得把预期写成实际。
6. **验证**：执行与修改风险相匹配的静态、语法、领域和范围检查。
7. **交付**：报告修改、规则或结论、验证结果、未处理的既有修改，以及是否执行 Git 发布动作。

## 12. 验证与交付要求

验证至少覆盖受影响文件；重命名、移动、导航、模板或全库规则变更必须扩大到所有可能受影响的入链和笔记。检查失败不得隐藏；若属于既有范围外问题，记录后保持不动。

必须按适用性执行：

- 运行 `git diff --check`，并对未跟踪的新文件补充等价的空白与行尾检查。
- 使用 YAML 解析器检查 Frontmatter；确认围栏、列表和字段类型合法。
- 检查 `created`、`updated` 是否为秒级 `YYYY-MM-DDTHH:mm:ss`，旧笔记的 `created` 是否保留。
- 检查笔记文件名、Frontmatter `title` 和正文职责是否一致；重命名时检查 `aliases` 与入链。
- 检查每个 Markdown 表格前是否有空行。
- 按文件名和 `aliases` 解析 Wikilink；带标题或块引用时还要检查目标锚点。扫描前必须排除代码围栏、行内代码和 Shell 的 `[[ ... ]]` 语法，避免误报。
- 检查 Mermaid 围栏、基本结构和需要引号的节点文本。
- 检查全部代码围栏闭合，且声明语言与内容一致。
- 对可执行 Shell 片段做适当语法检查，例如提取独立 Bash 片段后运行 `bash -n`；不得为了验证而直接执行有副作用的示例。
- 对 Compose、YAML、JSON、XML、SQL、Java、Go 或其他配置与代码示例执行适用的领域检查，例如 `docker compose config`、解析器、格式化器、编译器或测试。
- 对新增或易变化的官方链接做抽查或可访问性检查；无法联网时必须在交付中说明未验证。
- 搜索是否误写入密钥、Token、密码、私钥、真实凭据、无必要的个人绝对路径、动态 IP、设备标识或私人数据。
- 对比修改前后的 `git status` 和文件清单，确认没有范围外文件被修改、暂存、删除或生成。

验证应与风险匹配：纯措辞修改不必运行无关构建；命令、配置、Compose、源码示例、链接迁移或目录重构必须执行相应更深检查。无法适用的检查应明确记为“不适用”，不得伪造通过结果。

最终答复必须说明：

1. 创建、修改、移动或删除了什么。
2. 本次建立或调整了哪些核心规则、结构或事实。
3. 执行了哪些验证，各自结果如何；哪些检查不适用或无法完成。
4. 是否发现并保留了范围外修改或既有问题。
5. 是否暂存、提交或推送；未经明确要求时应明确说明均未执行。
