---
title: Linux 文件与目录常用命令
aliases:
  - Linux 文件操作命令
  - Linux 目录操作命令
  - Linux 路径与文件查找命令
tags:
  - Linux
  - Linux/文件与目录
  - Linux/命令行
  - Bash
  - 文件系统
created: 2026-07-19T23:47:26
updated: 2026-08-20T21:05:21
---

本文建立一套处理文件与目录的最小工作流：先定位当前上下文并只读核对目标，再执行最小范围的创建、复制、移动或删除，随后验证结果；若中途失败，则保留现场并判断恢复方式。目标不是背完所有参数，而是能够安全完成日常操作，并在忘记选项时知道怎样查证。

本篇默认使用 Ubuntu Server 中的 Bash 普通用户会话。阅读第 1～5 章前，应先理解 [[Shell 命令结构、类型与帮助系统]]、[[Shell 路径、变量、引用与展开]]，以及 [[Shell 标准流、管道、重定向与退出状态]] 中的退出状态和命令列表。第 6 章是综合练习，执行前还应能读懂 [[Shell 脚本阅读基础]] 中的条件、分支与圆括号分组。本篇只解释这些 Shell 结构在文件操作中的作用，不重复维护完整语法。

如何查看或搜索文件内容，见 [[Linux 文本查看、搜索与处理常用命令]]；权限和磁盘布局分别由对应专题负责。

## 1. 先理解路径与文件对象

Linux 把文件组织成一棵从 `/` 开始的目录树。命令操作的不是“屏幕上看到的名字”，而是某个路径经过 Shell 展开后指向的对象。

- **当前工作目录**：Shell 解释相对路径时采用的起点，可用 `pwd` 查看。
- **绝对路径**：从 `/` 开始，例如 `/etc/ssh/sshd_config`。
- **相对路径**：从当前工作目录开始，例如 `docs/readme.md`。
- `.`：当前目录。
- `..`：上一级目录。
- `~`：由 Shell 展开的当前用户主目录。

本篇主要涉及三类文件系统对象：普通文件保存数据；目录保存名称与下一级对象之间的对应关系；符号链接（symbolic link）保存一段指向其他路径的引用。符号链接指向的目标不存在时，称为失效符号链接。删除链接本身不会删除其目标，但会跟随链接的检查可能把失效链接判断为“不存在”。

相同命令对不同对象类型可能产生不同结果；例如复制目录通常需要递归选项。路径文字相同也不代表结果相同：只要当前工作目录发生变化，相对路径的含义就会随之改变。硬链接、访问控制列表（Access Control List，ACL）和扩展属性不在本篇展开。

## 2. 掌握目标与命令选择

本节只建立后续阅读地图，不要求在这里理解每条命令的选项；具体对象、语法和风险会在后文按使用顺序展开。

| 层级 | 应达到的程度 | 命令与能力 |
| --- | --- | --- |
| 必须熟练 | 能先核对目标，再完成基本文件操作并验证结果 | `pwd`、`cd`、`ls`、`mkdir`、`touch`、`cp`、`mv`、`rm` |
| 理解会查 | 知道它们回答什么问题，复杂参数按需查 | `rmdir`、`file`、`stat`、`realpath`、`basename`、`dirname`、`find`、`du`、`df` |
| 认识即可 | 看到时知道用途，不在本篇展开 | 符号链接、硬链接、访问控制列表（ACL）、扩展属性、`find` 的复杂表达式与动作 |

“必须熟练”不等于背下全部选项，而是能在执行前回答：源路径是什么、目标路径是什么、是否会覆盖、是否会递归、如何验证、失败后还能保留什么。

按问题选择命令，比按字母记忆更容易迁移：

| 要回答的问题 | 优先想到的命令 |
| --- | --- |
| 我在哪里，目录中有哪些对象 | `pwd`、`cd`、`ls` |
| 这个路径指向什么对象 | `file`、`stat`、`realpath` |
| 某类文件位于哪里 | `find` |
| 目录用了多少空间，所在文件系统还剩多少空间 | `du`、`df` |
| 创建、复制、移动或删除对象 | `mkdir`、`touch`、`cp`、`mv`、`rm`、`rmdir` |

忘记参数时，外部命令通常先查看 `COMMAND --help` 或 `man COMMAND`；`cd` 等 Shell 内建命令可以使用 `help cd`。命令结构和帮助系统的完整方法见 [[Shell 命令结构、类型与帮助系统]]。

## 3. 统一安全操作循环

文件操作不应从“我要输入哪条命令”开始，而应先建立同一套判断顺序：

```text
确认执行位置
    -> 识别源对象、目标位置和预期结果
    -> 判断会改变哪些对象以及能否恢复
    -> 执行最小范围的操作
    -> 检查退出状态和操作后状态
    -> 失败时保留现场并选择恢复方式
```

### 3.1 执行前核对

先区分四类经常被混在一起的风险：

| 风险 | 含义 | 为什么需要提前判断 |
| --- | --- | --- |
| 覆盖 | 现有目标可能被改写或替换 | 命令成功也可能造成旧内容丢失 |
| 递归 | 操作会进入目录并影响其下多层对象 | 实际范围可能远大于命令行中写出的一个路径 |
| 跨文件系统 | 源和目标由不同文件系统承载 | 移动可能不再只是改名，而会涉及复制和删除 |
| 删除 | 目录项或数据会被移除 | 命令通常没有通用撤销能力，恢复依赖事先建立的副本或机制 |

实际操作前至少确认以下问题：

1. 当前 Shell 位于哪个目录，相对路径将从哪里解析？
2. 源对象是否存在，它是文件、目录还是符号链接？
3. 目标路径上是否已有对象；如果有，它是什么类型？失效符号链接指向的对象虽不存在，但链接本身仍占用该路径。
4. 操作会改变哪些对象，是否包含覆盖、递归、跨文件系统或删除风险？
5. 计划用什么结果证明操作符合预期？
6. 如果中途失败，哪些现场需要保留，恢复依赖什么？

第 4 章会先逐个介绍只读观察工具，再在第 4.5 节把它们组合成可按任务裁剪的预检模板。

### 3.2 执行后验证

退出状态为 `0` 只表示命令认为自己成功完成，不自动证明结果符合人的意图。例如，预期把文件改成一个新名称，实际却把它放进了一个已有目录时，操作仍可能成功。

因此，变更后还要逐项核对后置条件：

1. 预期的新路径是否存在，对象类型是否正确？
2. 旧路径应该保留还是消失，它是否处于预期状态？
3. 重要内容或元数据是否需要额外比对？
4. 操作范围之外的对象是否保持不变？
5. 若命令失败，最后成功到哪一步，现场是否仍可用于判断和恢复？

第 4.5 节会在相关命令介绍完毕后给出后置条件模板；第 5 章再把这套循环应用到创建、复制、移动和删除。

### 3.3 按风险选择操作方式

| 操作性质 | 主要影响 | 执行前后的重点 |
| --- | --- | --- |
| 只读观察 | 不主动改变文件内容或目录项，但可能扫描大量对象或显示敏感路径 | 限定观察范围，判断输出是否完整并避免泄露 |
| 当前会话变化 | 改变当前目录或变量值，影响后续命令 | 变更后重新确认当前位置和变量值 |
| 创建或更新时间 | 新增对象，或改变已有对象的时间信息 | 核对父目录、名称是否空闲及最终对象类型 |
| 复制、移动或覆盖 | 写入目标，可能替换已有内容或消耗额外空间 | 分别核对源和目标，并验证内容、旧路径和新路径 |
| 删除 | 移除目录项或目录树 | 先确认恢复能力和精确范围，失败时保留现场 |

在系统目录中加入 `sudo` 不只是“让命令成功”，而是让变更以管理员权限发生。遇到权限错误时，应先检查对象归属、路径各级权限和正确管理方式，不要为了省事给日常项目操作加 `sudo`。详见 [[Linux 用户、用户组、sudo 与文件权限]]。

## 4. 只读观察与诊断

### 4.1 定位并查看目录

先确认当前位置，再进入 `$HOME` 指向的当前用户主目录并重新核对：

```bash
pwd
ls
ls -la
ls -ld -- "$HOME"
cd "$HOME"
pwd
```

- `pwd` 输出当前工作目录的绝对路径。
- `cd` 改变当前 Shell 的工作目录，因此它是 Shell 内建命令；子进程不能替父 Shell 改目录。
- `ls` 列出目录项；`-l` 使用长格式，`-a` 同时显示以 `.` 开头的名称。
- `ls -d` 在操作数是目录时显示目录项本身，而不是列出目录中的内容；`ls -ld -- PATH` 因此适合核对目标目录或符号链接本身的元数据。`--` 表示选项到此结束，后面的内容按操作数解释，可避免以 `-` 开头的名称被误当成选项。
- `cd -` 返回前一个工作目录；单独执行 `cd` 通常回到当前用户主目录。

`ls -l` 显示的是目录项元数据摘要，不等于文件内容，也不能证明当前用户真的可以完成某项操作。查看和搜索文件内容应使用 [[Linux 文本查看、搜索与处理常用命令]]；权限问题应结合 [[Linux 用户、用户组、sudo 与文件权限]] 判断。

### 4.2 识别对象并解析路径

```bash
file /etc/os-release
stat /etc/os-release
stat -c 'type=%F owner=%U:%G mode=%a size=%s path=%n' /etc/os-release
realpath /etc/os-release
basename /var/log/syslog
dirname /var/log/syslog
```

- `file` 读取内容特征，判断文本、可执行文件、压缩包等类型；它不只依赖扩展名。
- `stat` 显示类型、大小、权限、所有者和时间戳等元数据。
- `realpath` 规范化路径，并在允许的情况下解析其中的符号链接；缺失或不可访问的路径组件可能使它失败，应同时检查退出状态。
- `basename` 只取路径最后一段。
- `dirname` 只取最后一段之前的部分。

`basename` 和 `dirname` 处理的是路径字符串，不负责验证对象是否存在；`file`、`stat` 和 `realpath` 则会访问文件系统。需要核对符号链接时，不要只看 `realpath` 的最终结果，还要用 `ls -ld` 或 `stat` 区分链接本身和目标对象。

### 4.3 按条件定位对象

`find` 从起始路径向下遍历目录树，并对遇到的每个对象判断测试条件。初学阶段可以把命令按以下四部分阅读：

```text
find 起始路径 [遍历范围] [测试条件...] [动作]
```

| 部分 | 本节常用写法 | 含义 |
| --- | --- | --- |
| 起始路径 | `"$HOME/src"` | 从哪个目录开始；示例始终明确写出，不依赖省略时采用当前目录的默认行为 |
| 遍历范围 | `-maxdepth 2` | 起始路径作为第 0 层，最多处理到向下第 2 层 |
| 类型测试 | `-type f`、`-type d`、`-type l` | 分别匹配普通文件、目录和符号链接 |
| 名称测试 | `-name '*.log'` | 用 Shell 风格模式匹配路径最后一段名称，不匹配文件内容 |
| 时间测试 | `-mmin -60` | 匹配数据修改时间距现在不足约 60 分钟的对象 |
| 大小测试 | `-size +100M` | 按 MiB 单位筛选较大文件；结果按单位向上取整，适合寻找候选而非判断精确字节边界 |
| 动作 | `-print` | 把满足全部条件的路径写到标准输出 |

`find` 会逐个处理对象。相邻测试之间没有显式运算符时，相当于使用逻辑与（AND）：对象必须满足所有测试，才会执行后面的 `-print`。下面假设 `$HOME/src` 已存在；若实际工作区不同，应先替换并核对起始路径：

```bash
find "$HOME/src" -maxdepth 2 -type d -print
find "$HOME/src" -type f -name '*.log' -print
find "$HOME/src" -type f -mmin -60 -print
find "$HOME/src" -type f -size +100M -print
```

`-name` 只匹配路径最后一段名称；需要匹配整个路径时应另行查阅 `-path`，需要搜索文件内容时则使用文本搜索工具。`'*.log'` 必须引用，否则 Shell 可能在 `find` 运行前就在当前目录展开通配符，导致传给 `find` 的参数已经改变。`-mmin -60` 中的 `-60` 表示数值小于 60；时间测试按完整分钟或 24 小时时段计算，临界时间任务应先查 `man find` 中对应测试的取整规则。

读取结果时要同时区分三层证据：标准输出是已经匹配的路径，标准错误会报告无法访问或处理的对象，退出状态说明遍历是否完整成功。退出状态为 `0` 且没有匹配输出，表示在成功处理的范围内没有对象满足条件；若退出状态非零，即使已经输出了部分路径，也不能把结果当作完整清单。范围很大的起始路径还可能带来较长扫描时间或暴露敏感路径，因此应从明确、尽可能小的起点和深度开始。

初学阶段只使用只读的 `-print`。`-exec`、`-delete`、逻辑或与分组、目录剪枝以及面向任意文件名的批处理属于后续主题，不能在未重新核对范围和安全边界时机械替换 `-print`。

`find` 主要根据名称、类型、大小等条件查找文件系统对象；在文本内容中找匹配行通常使用 `grep` 或其他文本搜索工具，见 [[Linux 文本查看、搜索与处理常用命令#4.3 用 grep 找到需要的行|使用 grep 搜索文本]]。

### 4.4 检查目录占用与文件系统容量

第 1 章已经说明，`$HOME/src` 这样的目录位于 Linux 目录树中，其下还可以包含多层文件和子目录。这棵目录树中的内容最终需要由文件系统保存。

文件系统负责把一份存储空间组织成文件和目录，再接入 Linux 目录树。把文件系统接入目录树的动作称为挂载，接入位置称为挂载点。一个目录通常只是某个文件系统中的一部分，并没有一份独立于文件系统的总容量。因此要先区分两个问题：

1. `$HOME/src` 及其后代对象合计占用了多少空间？这由 `du` 回答。
2. 保存 `$HOME/src` 的整个文件系统还有多少可用空间？这由 `df` 回答。

下面假设 `$HOME/src` 已存在，所有命令都在 Ubuntu Server 的任意目录执行，并且只读取状态。

#### 用 `du` 估算目录树占用

`du` 是 disk usage 的缩写，用来估算一组文件占用的文件系统空间；操作数是目录时，它会递归处理目录树。默认会显示各级目录的累计结果，父目录的数值包含其后代对象占用的空间；若操作数本身是普通文件，则显示该文件的结果。

先查看整个目录树的汇总结果：

```bash
du -sh -- "$HOME/src"
```

再显示目标目录及其下一层子目录的累计结果：

```bash
du -h --max-depth=1 -- "$HOME/src"
```

| 选项 | 作用 |
| --- | --- |
| `-s`、`--summarize` | 每个操作数只显示一条汇总结果 |
| `-h`、`--human-readable` | 使用以 1024 为进位基础的易读单位 |
| `--max-depth=1` | 显示目标目录和向下一层子目录的累计结果 |
| `-x`、`--one-file-system` | 遇到接入其他文件系统的位置时不再进入 |

`-s` 与非零的 `--max-depth` 表达的是两种输出方式，不应组合使用。前者只保留总量，后者保留若干层目录的累计结果。

`--max-depth=1` 只限制显示到哪一层，不会停止读取更深层对象，因为 `du` 仍需遍历后代才能算出第一层目录的累计值。Linux 还允许在某个子目录上挂载另一个文件系统；若只想统计与起始目录属于同一文件系统的内容，可以增加 `-x`：

```bash
du -h -x --max-depth=1 -- "$HOME/src"
```

`du` 需要逐项遍历目录树，因此大目录或大量小文件都可能使扫描耗时。权限不足时，它可能已经输出部分结果，同时在标准错误报告无法访问的路径并以非零状态结束；此时结果不是完整清单，不应隐藏错误或未经判断就改用 `sudo`。

#### 用 `df` 观察所在文件系统

`du` 只回答选定目录树的占用。若要判断保存这些内容的整个文件系统是否还有空间，应使用 `df`。`df` 是 disk free 的缩写，用来报告已挂载文件系统的总容量、已用容量和可用容量：

```bash
df -hT -- "$HOME/src"
```

- `df -hT -- "$HOME/src"` 中，`-h` 使用易读单位，`-T` 增加文件系统类型。
- 常见输出列包括来源 `Filesystem`、类型 `Type`、总容量 `Size`、已用容量 `Used`、可用容量 `Avail`、使用率 `Use%` 和挂载点 `Mounted on`。
- 路径 `$HOME/src` 只用于指定要查询哪个文件系统。`df` 不会递归扫描这个目录，也不会把该目录的占用单独列出来；同一文件系统中的不同路径通常会显示相同的整体容量。

一组实际输出及各列含义见 [[Linux 磁盘、分区、文件系统与 LVM 基础#4.1 先确认路径属于哪个文件系统|从路径确认文件系统]]。`df` 只观察已经接入目录树的文件系统，不代表整块磁盘的全部容量；磁盘、分区和文件系统之间的关系见 [[Linux 磁盘、分区、文件系统与 LVM 基础#4.4 把结果串成一条检查顺序|存储链检查顺序]]。

#### 容量有剩余时还要检查什么

保存文件内容需要可用空间，创建新的文件或目录还需要文件系统为新对象保存一条记录。许多 Linux 文件系统把这种记录称为 inode（index node，索引节点）。因此，“还能保存多少内容”和“还能创建多少对象”是同一个文件系统上的两种不同限制。

`df -i` 不再显示内容所用的容量，而是显示 inode 的总数、已用数量、可用数量和使用率：

```bash
df -i -- "$HOME/src"
```

若 `df -hT` 显示仍有可用容量，但创建新文件或目录仍然失败，可以再用 `df -i` 检查 inode。`df -hT` 与 `df -i` 是对同一个文件系统的两种并列检查，inode 不是从文件系统到目录之间的下一层。

#### 按问题组合命令

前面的命令不是一条每次都必须全部执行的固定链条。先说明问题，再选择对应命令：

| 要回答的问题 | 使用的命令 |
| --- | --- |
| `$HOME/src` 整个目录树占用多少空间 | `du -sh -- "$HOME/src"` |
| 哪个第一层子目录占用较多 | `du -h -x --max-depth=1 -- "$HOME/src"` |
| 保存该目录的文件系统还有多少容量 | `df -hT -- "$HOME/src"` |
| 容量仍有剩余但无法创建新对象 | `df -i -- "$HOME/src"`，再检查写入权限和文件系统是否只允许读取 |

`df` 的 `Used` 描述整个文件系统，`du` 描述指定目录树，两者不是同一项统计，也不要求数值相等。发现差异时，按下面的边界判断：

| 差异来源 | 应怎样理解 |
| --- | --- |
| 观察范围不同 | `du` 可能只扫描文件系统中的一个目录，其他目录的占用仍会进入 `df` |
| 遍历不完整 | 权限错误、读取错误或预先使用的 `-x` 可能使 `du` 没有覆盖预期范围 |
| 已删除文件尚未释放 | 运行中的程序仍保持文件打开时，空间尚未归还文件系统，但 `du` 已无法从目录树找到该文件 |
| 文件系统自身也会用空间 | 文件系统需要保存管理信息，还可能保留一部分不提供给普通用户的空间 |

若 `df -hT` 显示文件系统容量紧张，可以用 `du -sh` 核对候选目录，再用 `--max-depth=1` 逐层定位；若容量仍充足却无法创建对象，再检查 `df -i`、写入权限和文件系统是否只允许读取。所有检查都应从明确、尽可能小的路径开始，并保留标准错误和退出状态作为结果是否完整的证据。

### 4.5 把只读工具组合成预检与后置验证

前面已经分别说明了当前位置、对象类型、真实路径、查找范围和容量边界。现在再把这些工具组合起来，读者不需要跳到后文猜测每条命令的作用。下面都是可按任务裁剪的结构，不是每次操作都要机械执行的固定清单。

#### 操作前预检

Bash 内建的 `printf` 支持 `%q` 格式，它会把参数输出成可供 Shell 重新读取的转义形式，便于发现路径中的空格、换行或其他特殊字符。它只负责显示变量当前保存的值，不能证明该路径安全或对象存在。

```text
pwd
printf '%q\n' "$PATH_TO_CHECK"
ls -ld -- "$SOURCE_PATH"
ls -ld -- "$DESTINATION_PATH"
file -- "$SOURCE_PATH"
stat -- "$SOURCE_PATH"
realpath -- "$SOURCE_PATH"
```

变量名是说明性占位符，必须先赋为已经核对的真实路径，不能把整段原样执行。目标预期尚不存在时，针对目标的 `ls` 返回非零状态可能正是预期结果；`file`、`stat` 或 `realpath` 遇到缺失、失效链接或不可访问的路径也可能失败。这些状态应进入判断，不能为了得到整齐输出而隐藏。

实际任务应根据问题选择命令：核对目录项本身优先使用 `ls -ld` 或 `stat`；判断内容类型使用 `file`；需要规范化并跟随符号链接时再使用 `realpath`。只看 `realpath` 的最终目标，不能代替对符号链接本身的检查。

#### 操作后验证

`test` 通过退出状态表达条件真假。`-e`、`-f` 和 `-d` 都会跟随符号链接，分别判断路径最终指向的对象是否存在、是否为普通文件、是否为目录。`-L` 则不跟随符号链接，而是判断参数指定的路径本身是否为符号链接。因此，如果链接目标不存在，即使链接本身仍在，`test -e`、`test -f` 和 `test -d` 也会返回假，而 `test -L` 仍会返回真。在路径可以正常检查的前提下，要确认该路径既没有指向现有对象，也没有遗留符号链接，必须同时确认 `test -e` 和 `test -L` 都返回假。

```text
test -e "$EXPECTED_PATH" || test -L "$EXPECTED_PATH"
test ! -e "$REMOVED_PATH" && test ! -L "$REMOVED_PATH"
ls -ld -- "$RESULT_PATH"
stat -- "$RESULT_PATH"
find "$CHECK_ROOT" -maxdepth "$MAX_DEPTH" -print
```

若结果必须是普通文件、目录或符号链接，应分别改用 `test -f`、`test -d` 或 `test -L`，不能只证明“某种对象存在”。`&&` 只在左侧成功时继续，`||` 只在左侧失败时继续；完整条件组合见 [[Shell 脚本阅读基础#6. 使用 test 表达条件|test 条件判断]]，退出状态和命令列表见 [[Shell 标准流、管道、重定向与退出状态]]。

后置验证必须围绕预期结果选择：复制要检查目标类型和必要的内容一致性；移动要同时检查新、旧两端；删除则要证明目标目录项消失。看到同名路径或一次退出状态为 `0`，都不能单独证明人的意图已经实现。

## 5. 创建、复制、移动与删除

下面开始执行会改变文件系统状态的操作。语法骨架使用中文占位符，因此声明为 `text`，不能原样执行；实际操作时应代入已经核对的路径，并按照“预检 → 操作 → 后置验证”完成闭环。

| 命令 | 产生的变化 | 首要风险 |
| --- | --- | --- |
| `mkdir`、`touch` | 创建对象或更新时间戳 | 落点错误；已有对象被改变 |
| `cp` | 保留源对象并写入目标 | 覆盖目标、空间不足、元数据不能完整保留 |
| `mv` | 改变名称或位置 | 目标解释错误、覆盖、跨文件系统时涉及复制与删除 |
| `rm`、`rmdir` | 删除文件、链接或目录 | 影响范围过大，且通常不能直接撤销 |

### 5.1 创建文件和目录

`mkdir` 创建目录；`mkdir -p` 还会创建路径中缺失的父目录。目标已经作为目录存在时，`-p` 不会因此报错，但路径中的已有对象若不是目录，命令仍会失败。`touch` 的本义是更新时间戳，目标不存在时通常会创建空文件；目标已经存在时，它会改变时间信息。

执行前先用 `pwd` 和 `ls -ld -- 父目录路径` 核对落点，并确认最终名称是否已经被占用：

```text
mkdir 目录路径
mkdir -p 多级目录路径
touch 文件路径
```

创建后使用 `test -d 目录路径`、`test -f 文件路径` 或 `stat` 检查对象类型。`touch` 不会为文件写入正文，也不是编辑器，不能把它当成完全无影响的“存在性检查”。

### 5.2 复制文件或目录树

`cp` 保留源对象并创建副本。日常使用主要掌握下面几种写法：

| 目的 | 示例 | 结果 |
| --- | --- | --- |
| 把文件复制为新名称 | `cp -- report.txt report.bak` | 创建 `report.bak`；若已存在，内容可能被改写 |
| 把文件放入已有目录 | `cp -- report.txt backup/` | 实际目标是 `backup/report.txt` |
| 把目录复制为新名称 | `cp -a -- project project.backup` | `project.backup` 不存在时，创建完整目录副本 |
| 把目录放入已有目录 | `cp -a -- project backups/` | 创建或合并 `backups/project/` |
| 只复制目录中的内容 | `cp -a -- project/. snapshot/` | 把内容直接合并进已有的 `snapshot/` |

`-a` 是 archive（归档）模式，会递归复制目录，并尽量保留符号链接、权限和时间等属性；普通 `cp` 默认不复制目录。目标是已有目录时，`cp` 会在其中追加源路径的最后一个名称，所以执行前不仅要检查 `backup/`，还要检查真正可能被写入的 `backup/report.txt` 或 `backups/project/`。

源路径不存在时，`cp` 会失败。目标路径不存在且父目录存在时，`cp` 通常直接用该名称创建副本；父目录不存在时也会失败。普通文件复制到已有文件时可能改写内容；目录复制到已有同名目录时会合并，而不是先清空目标，无关的旧内容仍会保留。递归复制中途失败时，目标还可能已经产生部分结果。

符号链接需要额外留意：普通 `cp` 复制源链接时通常读取它指向的对象，`cp -a` 则保留链接本身；目标是指向目录的链接时，会被当作该目录使用，目标是指向普通文件的链接时，则可能改写链接所指文件。遇到链接或失效链接时，先用 `ls -ld` 和 `realpath` 核对链接本身与最终路径；需要更细的控制时再查看 `man cp`。

复制后应检查退出状态和真正的目标路径，再确认对象类型、目录层级及必要的文件内容。失败时先保留错误输出并检查已有结果，不要未经判断就追加 `-f`、改用 `sudo` 或直接重试。

### 5.3 移动或重命名

`mv` 可以重命名，也可以把对象移动到另一个目录。与 `cp` 一样，已有目录会改变目标解释，已有文件还可能面临覆盖风险，因此必须先分别核对源和目标。

同一文件系统内的普通重命名通常只改变目录项；源和目标位于不同文件系统时，`mv` 可能变成“复制到目标后再删除源对象”，不再是同一文件系统中的原子重命名。无法确认边界或迁移重要目录时，优先采用“复制 → 验证目标 → 单独处理源目录”的可恢复流程。

```text
mv 源路径 目标路径
```

操作后应同时验证两项：新路径符合预期，旧路径也处于预期状态。只检查其中一端，无法证明移动或重命名完整完成。

### 5.4 删除明确目标

`rm` 删除文件或符号链接，默认不能删除目录；`rmdir` 只删除空目录，可以把“目录必须为空”作为保护条件；`rm -r` 会递归进入并删除整个目录树，影响范围明显更大。

删除不存在通用的命令级回滚。恢复通常依赖备份、版本控制、快照或应用自身的恢复机制，因此恢复能力必须在删除前建立，而不是删除后补救。

> [!warning] `rm` 默认不会进入回收站
> 在服务器终端中，删除通常不可直接撤销。不要对尚未展开确认的变量、通配符、`/`、`$HOME` 或陌生目录运行递归删除。先用 `printf '%q\n' "$TARGET"`、`ls -ld -- "$TARGET"` 和 `find "$TARGET" -maxdepth 2 -print` 核对目标；确认存在有效备份或目标确实可重建后，再执行最小范围删除。

完成上述检查后，才从与目标匹配的最小命令开始：

```text
rm -- 待删除文件
rmdir -- 待删除空目录
rm -r -- 待递归删除目录
```

`rm -f` 的主要作用是忽略不存在的文件并取消部分交互提示，不代表“更彻底”，也不会让危险操作更安全。删除后同时使用 `test ! -e 目标路径` 和 `test ! -L 目标路径` 验证目录项确实消失；如果删除失败，不要未经核对就升级为 `sudo rm -rf`，而应先检查对象类型、目录内容、权限和挂载边界。

## 6. 分阶段安全练习

下面的练习只操作 `mktemp` 创建的独立临时目录，不需要 `sudo`。请在同一个 Bash 会话中按顺序执行；每一阶段开始前先预测将看到什么，不要跳过路径核对。

> [!info] 执行前提
> 本章检验文件操作，不负责第一次教授 Shell 脚本语法。执行前应能读懂变量与命令替换、退出状态、`if`、`test`、`&&`、重定向和圆括号分组；若其中任一结构陌生，先回到开头列出的 Shell 专题完成对应最小主线。

这些结构在本练习中的职责如下：

| 结构 | 本练习中的作用 |
| --- | --- |
| `LAB_DIR="$(mktemp -d /tmp/file-command-lab.XXXXXX)"` | `mktemp -d` 创建目录并用六个随机字符替换 `XXXXXX`；命令替换取得实际路径并保存到当前 Shell 变量 |
| `if ...; then ... fi` | 根据命令或命令列表的退出状态选择成功、失败分支 |
| `( ... )` | 在子 Shell 中执行一组命令，使其中的 `cd` 不改变外层 Shell 的工作目录 |
| `&&` | 只有前一步成功才继续，避免中间失败后仍执行后续变更 |
| `>`、`>&2` | 分别把输出写入文件，或把错误提示发送到标准错误 |
| `${LAB_DIR:-}`、`[[ ... ]]` | 在变量未设置或为空时得到空值，并用 Bash 模式继续限制允许的清理路径 |

这里只说明场景职责。变量与命令替换的完整规则见 [[Shell 路径、变量、引用与展开]]；状态、命令列表和重定向见 [[Shell 标准流、管道、重定向与退出状态]]；分支、条件和子 Shell 见 [[Shell 脚本阅读基础]]。

### 6.1 创建练习目录并完成操作

代码块中的操作使用 `&&` 串联：任一步返回非零状态，后续步骤就不会继续。失败时临时目录会保留，便于检查，而不会自动清理证据。

```bash
if LAB_DIR="$(mktemp -d /tmp/file-command-lab.XXXXXX)"; then
  printf 'lab=%s\n' "$LAB_DIR"

  if (
    cd -- "$LAB_DIR" &&
    mkdir -p project/docs &&
    touch project/docs/readme.txt &&
    printf '%s\n' 'Linux command lab' > project/docs/readme.txt &&
    pwd &&
    ls -la project/docs &&
    file project/docs/readme.txt &&
    stat -c 'type=%F size=%s path=%n' project/docs/readme.txt &&
    cp project/docs/readme.txt project/docs/readme.backup.txt &&
    test -f project/docs/readme.backup.txt &&
    mv project/docs/readme.backup.txt project/docs/readme.old.txt &&
    test ! -e project/docs/readme.backup.txt &&
    test ! -L project/docs/readme.backup.txt &&
    test -f project/docs/readme.old.txt &&
    find project -maxdepth 3 -print &&
    find project -maxdepth 3 -type f -name '*.txt' -mmin -60 -print &&
    du -sh -- project &&
    du -h -x --max-depth=1 -- project &&
    df -hT -- "$LAB_DIR"
  ); then
    printf '%s\n' 'PASS: 操作阶段完成，练习目录已保留供复查。'
  else
    printf 'FAIL: 操作阶段中止，请检查并保留目录：%s\n' "$LAB_DIR" >&2
  fi
else
  printf '%s\n' 'FAIL: 无法创建练习目录，未执行后续文件操作。' >&2
fi
```

成功时，第一条 `find` 应显示 `project/`、`project/docs/`、`readme.txt` 和 `readme.old.txt`，但不再显示 `readme.backup.txt`；第二条 `find` 同时应用类型、名称和时间测试，应只显示刚创建的两个 `.txt` 文件。第一条 `du` 只显示 `project` 的汇总，第二条 `du` 还应显示 `project/docs`，并且父目录数值包含后代对象。`df` 的结果描述承载 `$LAB_DIR` 的整个文件系统，应能从 `Mounted on` 找到挂载点；这些具体数字取决于运行环境，也不要求与 `du` 相同。若出现 `FAIL`，先使用输出的 `lab=` 路径检查最后成功到哪一步，不要立即运行清理阶段。

### 6.2 删除前只读预览

`${LAB_DIR:-}` 在变量未设置或值为空时展开为空字符串，因此第一项 `test -n` 会失败并阻止后续操作。`[[ "$LAB_DIR" == /tmp/file-command-lab.?????? ]]` 使用 Bash 模式匹配；每个 `?` 代表恰好一个字符，用来要求路径符合本练习的固定前缀和六字符随机后缀。

路径形状本身不能证明目录来源正确，因此还要同时要求变量非空、目标是目录而不是符号链接，并在输出后由人核对实际路径和对象集合：

```bash
if test -n "${LAB_DIR:-}" &&
   test -d "$LAB_DIR" &&
   test ! -L "$LAB_DIR" &&
   [[ "$LAB_DIR" == /tmp/file-command-lab.?????? ]]; then
  printf 'cleanup-target=%q\n' "$LAB_DIR"
  find "$LAB_DIR" -maxdepth 3 -print
else
  printf '%s\n' 'FAIL: 练习目录校验失败，拒绝预览和删除。' >&2
fi
```

只有当输出路径和对象集合都符合预期，并且不再需要保留失败现场时，才继续清理。

### 6.3 按明确顺序清理并验证

下面沿用上一节的变量、对象类型和路径模式保护。本练习故意不使用 `rm -r`：先删除两个已知文件，再用 `rmdir` 逐层删除空目录；若出现额外对象，`rmdir` 会失败并保留现场。

```bash
if test -n "${LAB_DIR:-}" &&
   test -d "$LAB_DIR" &&
   test ! -L "$LAB_DIR" &&
   [[ "$LAB_DIR" == /tmp/file-command-lab.?????? ]]; then
  if rm -- "$LAB_DIR/project/docs/readme.old.txt" &&
     rm -- "$LAB_DIR/project/docs/readme.txt" &&
     rmdir -- "$LAB_DIR/project/docs" &&
     rmdir -- "$LAB_DIR/project" &&
     rmdir -- "$LAB_DIR"; then
    printf '%s\n' 'PASS: 练习目录已按明确目标清理。'
  else
    printf 'FAIL: 清理未完成，请重新检查：%s\n' "$LAB_DIR" >&2
  fi
else
  printf '%s\n' 'FAIL: 练习目录校验失败，拒绝删除。' >&2
fi
```

最后执行：

```bash
if test -n "${LAB_DIR:-}" &&
   test ! -e "$LAB_DIR" &&
   test ! -L "$LAB_DIR"; then
  printf '%s\n' 'PASS: 练习目录已不存在。'
else
  printf '%s\n' 'FAIL: 无法证明练习目录已删除。' >&2
fi
```

## 7. 不看笔记自测

1. 当前目录为 `/home/dev/src` 时，`../logs` 指向哪里？
2. 一次文件变更的“执行前核对”和“执行后验证”分别要回答什么问题？
3. 为什么在复制或移动前，要先判断目标路径是否已经是目录？
4. `touch existing.txt` 为什么不算严格只读？
5. 什么时候应优先使用 `rmdir`，什么时候才需要 `rm -r`？
6. `find "$HOME/src" -type f -name '*.log' -print` 中的起始路径、测试条件和动作分别是什么？两个测试怎样组合，为什么名称模式要加引号？
7. `find` 没有输出时，为什么还要检查标准错误和退出状态？它与 `grep` 分别在找什么？
8. `basename`、`dirname` 与 `realpath` 是否都会检查对象存在？
9. `du -sh /var/log` 与 `du -h -x --max-depth=1 /var/log` 的输出范围有什么不同？`-x` 限制的又是什么？
10. 为什么 `df -hT /var/log` 不表示 `/var/log` 目录自身的大小？`df -i /var/log` 改为检查什么？
11. 为什么 `du` 的目录占用与 `df` 的文件系统已用容量不必相等？
12. 为什么命令退出状态为 `0`，仍不能完全替代对操作后状态的检查？

能用自己的话回答，并在临时目录中完成“预检 → 操作 → 验证 → 受控清理”，可以认为达到了本篇当前阶段的学习目标。记不住选项时使用帮助系统，是正常工作方式。

## 8. 常见误解

| 误解 | 正确认识 |
| --- | --- |
| `ls` 没显示就是目录为空 | 隐藏名称需要 `ls -a`；权限也可能限制读取 |
| `ls` 可以查看文件内容 | `ls` 查看目录项及其元数据摘要，文件内容应使用文本查看工具 |
| 文件扩展名决定类型 | Linux 不强制依赖扩展名，`file` 会检查内容特征 |
| `touch` 只是创建空文件 | 它也会更新已有对象的时间戳 |
| `cp -r` 与 `cp -a` 永远等价 | `-a` 还试图保留链接和多种元数据 |
| `mv` 永远只是改目录项 | 跨文件系统时可能包含复制与删除 |
| `rm -f` 更适合日常使用 | 它会减少保护性反馈，不会降低删除风险 |
| `find` 与 `grep` 都是在找文件 | `find` 主要定位文件系统对象，`grep` 主要匹配文本内容 |
| `find` 没有输出就证明目标不存在 | 只有在起始范围正确且遍历成功时，空输出才表示没有对象满足条件；错误可能使结果不完整 |
| `du` 给出的数值就是整块磁盘的精确占用 | 它只估算指定对象及其后代的占用，范围还会受到权限和挂载边界影响 |
| `du --max-depth=1` 只会读取一层目录 | 它限制输出层级，但仍需读取更深层对象来计算累计值 |
| `df PATH` 显示 `PATH` 自身大小 | 路径只用于选择所在文件系统；目录树占用应使用 `du` |
| `df -hT` 有剩余空间就一定能创建文件 | 还要检查 inode、写入权限和文件系统是否只允许读取 |
| `du` 和 `df` 应给出同一个数字 | 两者观察范围和统计来源不同，差异本身不能证明任一命令出错 |
| `test ! -e PATH` 能证明任何类型的目录项都不存在 | 它可能把失效符号链接判断为不存在；还要结合 `test ! -L PATH` |
| 退出状态为 `0` 就证明结果符合意图 | 它只证明命令认为执行成功，仍需核对目标路径和后置条件 |
| 路径以 `/` 开头只是写法不同 | 它是从根目录解析的绝对路径，与当前目录无关 |

## 相关笔记

- [[Linux 命令行学习路线与命令地图]]
- [[Shell 命令结构、类型与帮助系统]]
- [[Shell 路径、变量、引用与展开]]
- [[Shell 标准流、管道、重定向与退出状态]]
- [[Shell 脚本阅读基础]]
- [[Linux 文本查看、搜索与处理常用命令]]
- [[Linux 用户、用户组、sudo 与文件权限]]
- [[Linux 开发工作区与本地文件系统规划]]
- [[Linux 磁盘、分区、文件系统与 LVM 基础]]

## 官方参考资料

以下资料按括号中的日期核对：

- [GNU Coreutils Manual](https://www.gnu.org/software/coreutils/manual/coreutils.html)（2026-08-20）
- [GNU Coreutils Manual：cp](https://www.gnu.org/software/coreutils/manual/html_node/cp-invocation.html)（2026-08-20）
- [GNU Coreutils Manual：Target directory](https://www.gnu.org/software/coreutils/manual/html_node/Target-directory.html)（2026-08-20）
- [GNU Coreutils Manual：du](https://www.gnu.org/software/coreutils/manual/html_node/du-invocation.html)（2026-08-20）
- [GNU Coreutils Manual：df](https://www.gnu.org/software/coreutils/manual/html_node/df-invocation.html)（2026-08-20）
- [Ubuntu Manpage：du](https://manpages.ubuntu.com/manpages/noble/man1/du.1.html)（2026-08-20）
- [Ubuntu Manpage：df](https://manpages.ubuntu.com/manpages/noble/man1/df.1.html)（2026-08-20）
- [GNU Findutils Manual](https://www.gnu.org/software/findutils/manual/)（2026-08-20）
- [Ubuntu Manpage：find](https://manpages.ubuntu.com/manpages/noble/en/man1/find.1.html)（2026-08-20）
- [Ubuntu Manpage：file](https://manpages.ubuntu.com/manpages/noble/en/man1/file.1.html)（2026-07-19）
- [Ubuntu Manpage：realpath](https://manpages.ubuntu.com/manpages/noble/en/man1/realpath.1.html)（2026-07-19）
